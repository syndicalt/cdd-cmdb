# minimal-fastapi

> Auto-generated CMDB implementation — **minimal** profile, **python/fastapi/sqlite** backend.

This code was generated by an LLM from the [cdd-cmdb](https://github.com/syndicalt/cdd-cmdb) test specification.
It is not hand-written. Regenerate it anytime by running:

```bash
python -m generator --profile minimal --backend "python/fastapi/sqlite"
```

## Run it

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

## Validate it

```bash
cd ../..  # back to repo root
CMDB_BASE_URL=http://localhost:8080 pytest -c profiles/minimal.ini
```

---
*Generated on $(date -u +%Y-%m-%dT%H:%M:%SZ) by [cdd-cmdb generator](https://github.com/syndicalt/cdd-cmdb)*
