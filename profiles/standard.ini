[pytest]
# Standard profile: core + discovery + audit + graph + search + diff + reconciliation.
testpaths = suites/core suites/discovery suites/audit suites/graph suites/search suites/diff suites/reconciliation
addopts = --tb=short -q
